/**
 * Configuration module - central export for all design tokens and constants.
 *
 * Import from this file to access any configuration value:
 * ```ts
 * import { BREAKPOINTS, SPACING, ANIMATION } from '@/config';
 * ```
 */

export * from './breakpoints';
export * from './designTokens';
